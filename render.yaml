services:
  - type: web
    name: taskmaster-api
    runtime: node
    repo: https://github.com/SachiSagar5/TodoList
    plan: free
    buildCommand: npm install --prefix server
    startCommand: node server/index.js
    healthCheckPath: /
    envVars:
      - key: NODE_VERSION
        value: 18
